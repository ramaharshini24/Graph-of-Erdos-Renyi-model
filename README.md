We set up N = 20 agents with random initial positions in R2. A connected communication network
G(V, E) ∼ G(N, p) is constructed using the Erd˝os–R´enyi model, regenerating the graph until connectivity
is satisfied. To drive each agent i to its designated target position in a given letter shape, we use a
proportional control law. The target formation x∗ updates sequentially once the agents converge onto each
letter:
Initial −→ H −→ A −→ R −→ S −→ H −→ I −→ N −→ I
